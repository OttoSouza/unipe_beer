import { makeStyles } from "@material-ui/core/styles";


const CardStyles = makeStyles({
    root: {
      maxWidth: 345,
    },
    media: {
      height: 140,
    },
  });

export default CardStyles;
